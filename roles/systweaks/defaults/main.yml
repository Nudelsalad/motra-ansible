---
# Defaults for kernel role

# Directory containing the kernel .deb packages
kernel_deb_dir: "{{ playbook_dir }}/kernel"
# Optional: expected kernel version to assert after reboot
# Leave undefined to skip assertion
# kernel_expected_version: "6.12.0-rc5-v8-16k+"
